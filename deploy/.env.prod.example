# Deployment settings for the VPS (docker-compose.prod.yml)
# Copy to deploy/.env.prod on the server and fill in real values.
SITE_URL=pmautomation.duckdns.org
DUCKDNS_DOMAIN=pmautomation
DUCKDNS_TOKEN=
